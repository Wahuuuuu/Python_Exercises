#
# @lc app=leetcode id=3453 lang=python3
#
# [3453] Separate Squares I
#

# @lc code=start
class Square:
    def __init__(self, x, y, l):
        self.x = x
        self.y = y
        self.side = l
        

class Solution:
    def init_lower_higher(self, squares: list[list[int]]) -> tuple[Square]:
        


    def separateSquares(self, squares: list[list[int]]) -> float:
        lower, higher = self.init_lower_higher(squares)  # square which's base is lower and higher
        

# @lc code=end

